import 'package:flutter/material.dart';

class ChatScreenLoadingAppBar extends StatelessWidget {
  const ChatScreenLoadingAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
